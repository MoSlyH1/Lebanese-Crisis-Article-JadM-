# Copy to .env for local non-Docker runs. docker-compose sets these for you.
DATABASE_URL=postgres://lebanon:lebanon@localhost:5432/lebanon
NODE_ENV=development
# Set to "false" to disable SSL (local). On Render, leave unset (defaults to SSL in production).
DATABASE_SSL=false
PORT=3000
